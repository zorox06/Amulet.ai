{
  "name": "guardian-stripe-demo",
  "private": true,
  "version": "1.0.0",
  "description": "Static-analysis-only legacy Stripe fixture; never run against a real key.",
  "dependencies": {
    "stripe": "8.220.0"
  }
}
